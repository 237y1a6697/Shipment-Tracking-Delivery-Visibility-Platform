import {useState} from 'react';
const API='http://localhost:8080/api/auth';
function App(){
 const [mode,setMode]=useState('login'); const [form,setForm]=useState({name:'',email:'',password:''}); const [msg,setMsg]=useState(''); const [loading,setLoading]=useState(false);
 const submit=async e=>{e.preventDefault();setMsg('');setLoading(true);try{const r=await fetch(API+'/'+mode,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw Error(d.message||'Something went wrong');localStorage.setItem('shiptrackToken',d.token);localStorage.setItem('shiptrackUser',JSON.stringify(d));setMsg('Success! Welcome, '+d.name+'.');}catch(err){setMsg(err.message)}finally{setLoading(false)}};
 return <main className="page"><section className="card"><div className="brand"><div className="logo">S</div><div><h1>ShipTrack Pro</h1><p>Shipment Tracking & Delivery Visibility</p></div></div><div className="tabs"><button className={mode==='login'?'active':''} onClick={()=>{setMode('login');setMsg('')}}>Login</button><button className={mode==='register'?'active':''} onClick={()=>{setMode('register');setMsg('')}}>Register</button></div><h2>{mode==='login'?'Welcome back':'Create your account'}</h2><p className="sub">{mode==='login'?'Sign in to manage and track shipments.':'Register to start managing your shipments.'}</p><form onSubmit={submit}>{mode==='register'&&<label>Full name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name"/></label>}<label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input type="password" required minLength="6" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Minimum 6 characters"/></label><button className="submit" disabled={loading}>{loading?'Please wait...':mode==='login'?'Login':'Create account'}</button></form>{msg&&<div className={msg.startsWith('Success')?'success':'error'}>{msg}</div>}<p className="footer">Secure authentication • Spring Boot • PostgreSQL • JWT</p></section></main>;}
export default App;